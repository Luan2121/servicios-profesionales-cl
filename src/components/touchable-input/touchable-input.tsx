import React from 'react';
import { View, TextInput, TextInputProps } from 'react-native';
import { Box } from 'bumbag-native';

interface Props extends TextInputProps {

}

const TouchableInput = (props : Props) => {
    return(
        <Box>
            
        </Box>
    );
}

export { TouchableInput }